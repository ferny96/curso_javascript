//Como crear un array(arreglos) con JavaScript

const numeros = [1, 2, 3, 4, 5, 6];

const meses = new Array('enero', 'febrero', 'marzo', 'abril', 'mayo');

console.table(numeros);
console.table(meses);

//Arreglo con datos de todo tipo

const detodo = ['hola', 10, true, "si", null, {nombre:'fernando', cargo:'estudiante'}];

console.log(detodo);